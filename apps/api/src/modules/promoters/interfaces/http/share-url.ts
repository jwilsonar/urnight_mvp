/**
 * Enlaces compartibles de códigos (`/p/<code>`). La base pública y su
 * construcción viven en `application/config/web-url` (punto único, §M19); aquí
 * sólo se reexporta para la capa de presentación.
 */
export { shareUrlFor } from '../../application/config/web-url';
