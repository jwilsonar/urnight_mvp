/** Mensajes y constantes compartidas de la app web. */

/** Mensaje único para sesión expirada / token ausente (evita duplicación literal). */
export const SESSION_EXPIRED = 'Tu sesión expiró. Inicia sesión nuevamente.';
