# pnpm por defecto usa node-linker=isolated; Metro (Expo SDK 52+) lo soporta.
# (hoisted causaba que react-native no se materializara en disco.)
auto-install-peers=true
strict-peer-dependencies=false
save-workspace-protocol=rolling
